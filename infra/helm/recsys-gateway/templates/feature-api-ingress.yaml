{{- if .Values.featureApi.enabled }}
{{- $featureApiRateLimit := .Values.featureApi.rateLimit | default dict }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recsys-online-feature-api-gateway
  namespace: {{ .Values.featureApi.namespace }}
  annotations:
    {{- if .Values.auth.enabled }}
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: {{ .Values.auth.secretName }}
    nginx.ingress.kubernetes.io/auth-realm: {{ .Values.auth.realm | quote }}
    {{- end }}
    nginx.ingress.kubernetes.io/service-upstream: "true"
    nginx.ingress.kubernetes.io/upstream-vhost: {{ .Values.featureApi.upstreamHost | default (printf "%s.%s.svc.cluster.local" .Values.featureApi.service.name .Values.featureApi.namespace) | quote }}
    {{- if ($featureApiRateLimit.enabled | default false) }}
    nginx.ingress.kubernetes.io/limit-rps: {{ $featureApiRateLimit.rps | quote }}
    nginx.ingress.kubernetes.io/limit-rpm: {{ $featureApiRateLimit.rpm | quote }}
    nginx.ingress.kubernetes.io/limit-connections: {{ $featureApiRateLimit.connections | quote }}
    nginx.ingress.kubernetes.io/limit-req-status-code: {{ $featureApiRateLimit.statusCode | quote }}
    {{- end }}
    {{- if .Values.tls.enabled }}
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: {{ .Values.tls.clusterIssuerName | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.gateway.ingressClassName }}
  {{- if .Values.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.featureApi.host }}
      secretName: {{ .Values.tls.secretNames.featureApi }}
  {{- end }}
  rules:
    - host: {{ .Values.featureApi.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.featureApi.service.name }}
                port:
                  number: {{ .Values.featureApi.service.port }}
{{- end }}
