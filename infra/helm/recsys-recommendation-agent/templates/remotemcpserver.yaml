{{- $mcpAuth := include "recsys-recommendation-agent.mcpAuthRotation" . | fromJson -}}
apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: {{ .Values.mcp.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    recsys.ai/mcp-auth-revision: {{ $mcpAuth.revision | quote }}
  labels:
    {{- include "recsys-recommendation-agent.labels" . | nindent 4 }}
spec:
  description: Authenticated recommendation-only MCP facade for recsys-inference-api.
  protocol: STREAMABLE_HTTP
  url: {{ $mcpAuth.url | quote }}
  timeout: {{ .Values.mcp.timeout }}
  headersFrom:
    - name: Authorization
      valueFrom:
        type: Secret
        name: {{ $mcpAuth.secretName }}
        key: {{ .Values.mcp.authSecretKey }}
