autoscaling:
  minReplicas: 2
  maxReplicas: 3
  metricMode: assignedWorkers
  assignedWorkersPerReplica: "0.7"
  # Retained for a values-only rollback to the Substrate 0.0.6 baseline.
  cpuTargetMicrocores: "400"
  fallback: {failureThreshold: 3, replicas: 1}
