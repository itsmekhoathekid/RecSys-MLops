sandbox:
  # Golden gVisor checkpoints are CPU-feature-sensitive. Rebuild the template
  # after moving the compact profile onto its pinned N2 worker node.
  modelConfigRevision: substrate-0.0.11-kagent-e6df917-compact-12vcpu-v1

autoscaling:
  minReplicas: 1
  maxReplicas: 1
  fallback:
    replicas: 1
